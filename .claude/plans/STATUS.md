# 진행 상태 — 세션 핸드오프

> 다음 세션은 **이 파일(STATUS) → (UI 작업이면) `2026-06-21-UI-마무리-작업목록.md` → `2026-06-20-통합계획서.md`(통합 계획서 — Part I 전체설계·로드맵 / Part II M2 / Part III M3)** 순으로 읽고 이어서 진행하세요.
> 계획·설계 문서는 모두 **이 디렉터리 `.claude/plans/`** 에 있다(2026-06-20 `docs/` 에서 이전 + 3개 계획서를 `2026-06-20-통합계획서.md` 하나로 병합). 필요한 Part/섹션만 읽으세요(전체 X).
> (메모리 스토어가 이 환경에서 깨져 있어 — bind-mount 이슈 — repo 노트로 핸드오프합니다.)

**마지막 업데이트: 2026-07-15 (팀장 직접 구현 — e1 종료 상태, 사용자 지시 "팀장이 다 작업". **M4b+M4c 완료·미커밋**: 국내·해외주식 LIVE 주문경로 배선. pytest **230 green**·ruff clean·allow_live=false 기본 실주문 0 검증. M4a 도 아직 미커밋 — working tree 에 M4a+M4b+M4c 누적. 브랜치 `chore/sandbox-web-port-8000`.)**

## 완료된 것
- **M4b+M4c 완료(2026-07-15, 팀장 직접 구현, 커밋 대기) — 국내·해외주식 LIVE 주문경로 배선(INV-1 첫 해제, allow_live=false 로 닫힌 채 코드 적재·실주문 0)**. 계획서 `.claude/plans/2026-06-20-M4-계획서.md` §4/§6/§7 대로. **라이브러리 소스 직접 검증**(CSPAT00601/00701/00801·CSPAQ13700/12300·COSAT00301/00311·COSAQ00102·COSOQ00201 InBlock/OutBlock 필드·facade 시그니처·응답 block 구조 실측; §16 정정 7건 반영). **신규**: `app/adapters/korea_stock_order.py`(KoreaStockOrderAdapter: BnsTpCode 1매도/2매수·OrdprcPtnCode 00지정/03시장·성공판정 OrdNo존재 '00000'강제금지·**MbrNo NXT/KRX venue 라우팅**·CSPAQ13700 list-all/CSPAQ12300 잔고)·`overseas_stock_order.py`(OverseasStockOrderAdapter: OrdPtnCode 01매도/02매수/08취소·COSAT00311 정정 07·OrdMktCode 81/82·성공 '00000'·심볼분해 재사용·COSAQ00102/COSOQ00201). **registry**: KR/OVS 등록 + `order_tr_labels()`(TR 리터럴은 어댑터에만 — 불변식 스코프 유지). **order_service 일반화**: trading_mode_of·tr_code 라벨·`_get_mutable` KR/OVS 허용+allow_live 게이트·cancel qty=미체결잔량·시장별 exchange 기본값. **gate**: settings 주입(allow_live 마스터)+step2 시장분기(FUT/LIVE/미지)+**step4' NO_NOTIONAL_FOR_LIVE(시장가 캡우회 하드거부)**+LIVE 소액 per-order 캡(KR KRW/OVS USD). **live reconcile**(`_reconcile_live`): KR/OVS list-all·**소멸판정 보류 fail-safe**(단일페이지 미완결→CANCELED/EXPIRED 안함)·OrgOrdNo 체인 orphan 오등록 차단·시장별 권위 writer·boot `_boot_live_bucket` 배선(allow_live=true+동기화성공→ACTIVE). **불변식 진화(삭제 아닌 정밀화)**: `test_readonly_invariant` = 발주 TR "app/ 어디에도 없음"→"전용 어댑터에만"(CSPAT→korea_stock_order/COSAT→overseas_stock_order, CIDBT 패턴 동일). **신규 테스트**: test_korea_order(10)·test_overseas_order(9)·test_live_gate(8)·test_live_reconcile(8, 체결적용·fail-safe·orphan체인·포지션동기화·allow_live 방어·**행동증명: allow_live=true+ACTIVE→캡통과분만 발사/false→실주문0**). **검증**: pytest **230 passed**·ruff clean·allow_live 기본 false(문 열림≠생성; make_order_adapter(KR/OVS,allow_live=false)→LIVE_DISABLED)·INV-1 매트릭스 무변·fresh-DB migration+app.main import OK. **⚠ 남은 것(M4d/M4f, 이번 범위 밖)**: killswitch LIVE 실동작(§9)·quote→commit 2단계 confirm_token(§10)·첫주문 완충가드·balances_snapshot 라이브 push(§15)·step4'' 누적 명목 캡·2층 커서 페이지네이션+소멸판정([L]/Gate B). **M4f(실발사)는 평일+계좌충전+명시 발사승인까지 HOLD(allow_live=false 유지).** **📖 책 반영 필요**: programgarden_hanbit_book ch07 §7.2.5·§7.4("아직 설계일 뿐 코드 아님")+ch08 실습을 실제 구현에 맞춰 그 책 세션에서 수정.
- **샌드박스 web 호스트 포트 13000 → 8000 전환 + 사이트 라이브 확인(2026-07-09, 커밋 `4247ba7`, 브랜치 `chore/sandbox-web-port-8000`)**. 사용자 요청("사이트를 localhost:8000 으로"). 호스트 8000 이 비어 있음을 실측 확인(리스닝 0·curl 거부) 후 전환 결정. **변경 5파일**: `sandbox/run-isolated.sh`(web publish `-p 8000:3000`, 폴백범위 `8001-8010`; **api 는 `18000:8000` 유지**) · `CLAUDE.md`·`sandbox/README.md`(새 매핑 + "컨테이너 안 `8000`=api / 호스트 `8000`=web" 번호 의미 충돌 주의 명시) · `server/.env.example`+`server/app/config.py`(CORS 허용목록에 `http://localhost:8000` 추가, 기존 13000/3000 유지 — web 이 same-origin 프록시라 실제론 안 걸리지만 방어적). `bash -n` 통과. **⚠ 포트 매핑은 컨테이너 생성 시점에 고정** → 현 컨테이너(`c7795347f455`, 수정 전 생성)는 여전히 `13000→3000`. **다음 컨테이너 재생성부터 8000 적용**(별도 작업 불필요). **재생성을 지금 안 한 이유**: 컨테이너의 메인 프로세스가 팀원 e1 의 claude 세션이라 `docker rm -f` 하면 e1 이 죽는다(사용자가 "일단 13000 에서 확인" 선택). **사이트 라이브 검증(호스트 실측)**: `localhost:13000` 200 · `localhost:13000/charts` 200 · `localhost:18000` 404(루트 라우트 없음 — 정상). **⚠ 세션 교훈**: 사이트가 안 뜬 원인은 포트가 아니라 **개발서버가 한 번도 안 떠 있었던 것**(포트 전환을 위해 기동을 보류시켰다가 재개를 잊음). 포트 의심 전에 `curl` 로 리스너 유무부터 확인할 것.
- **UI 마무리 Wave 1 완료(2026-06-21, 커밋 `fa06753`) — 명명 미구현 잔여 3/4 마감 + 고가치 버그**. 화면별 적대적 분석(워크플로우 8에이전트: 6화면+공통셸/모달+명명갭/백엔드) → 우선순위 도출 → Wave 1 구현. **① 서버시계**: `ServerClock` 이 `new Date()` 로컬 → `/system/clock` 서버 권위시각+skew 보정(세션은 M0 스텁이라 '세션 미상' 정직표기). **② HKEX 화이트리스트 검증**: 신규 `GET /orders/whitelist`(게이트 강제 화이트리스트 노출, multiplier=meta_json 파싱) → 모달 심볼 자유텍스트→검증 select(47종목)+빈심볼/수량≤0/가격≤0 차단. **③ Orders 페이지네이션**: history 80고정→"더 보기(+50)"(서버 limit/offset, limit 을 쿼리키에 포함)+필터0건/데이터0건 구분 "필터 초기화" CTA. **🐛 Positions 시세없음**: `current_price=null` 행이 합계 조용히 0 왜곡→'시세 없음'+합계 제외+⚠. **남은 명명갭=④ Charts SL/TP 오버레이뿐→M4 보류가 맞음**. **변경 9파일**(server `app/api/orders.py` + web `ServerClock`·`OrderTicketModal`·`orders/page`·`PositionsView`·`lib/api/{types,endpoints}`·`lib/query/{keys,hooks}`). **검증**: `ruff` clean·`tsc` clean·`eslint --max-warnings 0` clean·서버 **191 passed**·새 엔드포인트 200(프록시)·6라우트 200·백엔드 재시작(새 라우트 라이브). **상세 작업목록·화면별 현재상태·남은 Wave 2~4 = `.claude/plans/2026-06-21-UI-마무리-작업목록.md`**(교재 챕터 기준).
- **Overview 등 HTTP 404 근본해결 — same-origin API 프록시 전환(2026-06-20, 커밋 `993faec`)**. **근본원인(앞 세션 진단보다 한 겹 깊음)**: 컨테이너 안 curl 은 모든 M3 엔드포인트 200 인데 **호스트 브라우저의 `localhost:8000` 이 이 컨테이너 M3 서버가 아님** → 404. 이유=호스트 8000 포트가 다른 컨테이너/프로젝트에 점유돼 이 컨테이너 :8000 은 다른 호스트 포트로 매핑(web 이 3003 폴백한 것과 동일 현상) **또는 Tailscale 원격**(`next.config` `allowedDevOrigins` 단서 — 그 경우 브라우저의 localhost 는 사용자 기기). 앞 세션의 '서버 좀비 교체로 해결' 은 컨테이너 내부만 보고 한 오판이었음. **해결=`localhost:8000` 가정을 코드에서 제거하고 same-origin 프록시로 전환**(브라우저는 이미 잘 닿는 web origin 으로만 API/WS 호출 → web 서버가 같은 컨테이너 :8000 으로 프록시 → 호스트 포트/원격 무관, 컨테이너 안에서 완전 검증 가능). **변경 4파일**: `web/next.config.ts`(`rewrites()` `/api/v1/:path*`→`${BACKEND_ORIGIN:-http://localhost:8000}`; WS 업그레이드도 Next16 Turbopack 이 프록시함 — 실측) · `web/src/lib/api/client.ts`(브라우저 베이스 상대 `/api/v1`, `wsUrl()` 페이지 origin 에서 `ws(s)://` 절대유도, `restBase()` SSR 안전 폴백=서버측 컨테이너 내부 절대주소) · `web/src/lib/modes.ts`(TopBar=서버컴포넌트 SSR 이라 `BACKEND_ORIGIN` 내부 절대주소) · `web/src/app/page.tsx`(**`anyError` 전체화면 조기 return 제거→KPI/통화카드/손익 섹션별 로딩·에러 격하**; 단일 엔드포인트 실패가 화면 전체를 안 막음. 나머지 5화면은 이미 섹션별 처리라 무변). **코드 기본값도 same-origin 으로 고쳐 gitignore 된 `web/.env.local` 없이도 동작**(단 `.env.local` 도 `/api/v1`+`BACKEND_ORIGIN` 로 갱신함). **검증(컨테이너 내, 브라우저와 동일 경로 web:3000 경유)**: HTTP 6엔드포인트(health/halt_state/portfolio/positions/accounts/orders) 200+`ok:true` · WS `ws://:3000/api/v1/stream` open+info(M3)+ping/pong · SSR `fetchModes` 라이브(백엔드 로그 `/system/modes` 200·폴백 아님) · 6라우트 200·에러마커 0 · `tsc --noEmit` clean · `eslint . --max-warnings 0` clean. **dev 서버는 새 config/env 로 재기동함**(컨테이너 :3000). **✅ 호스트 실브라우저 육안확인 완료(2026-06-20, 사용자 "잘 뜬다")**: 하드리프레시(Cmd+Shift+R) 후 6화면 데이터 정상 렌더 — 404 근본해결 최종 확정. (커밋 직전 재현검증도 green: web:3000 프록시 6엔드포인트 200·6라우트 200·WS open+info(M3)+ping/pong.) **⚠ `next build` 미실행**(구동 중 dev 서버의 `.next` 충돌 회피; tsc+dev 컴파일로 대체).
- **web 대시보드 UI 실연결 완성(2026-06-20, 커밋 `af63a88`) — 사용자 지시 "우선 ui부터 완성"(교육용 예제→교재화 예정)**. 충실도 결정=**실연결+샘플 시드+'M4 예정' 배지**(목데이터 X·교육 정직성). **신규 토대**: `web/src/lib/api/`(엔벨로프 `{ok,data,error}` 클라이언트+`ApiError`+DTO 타입+엔드포인트 함수)·`lib/query/`(React Query 훅 reads+mutations+`Providers` layout 배선)·`lib/ws/`(단일 WS 클라이언트 `/api/v1/stream` 재연결·seq갭 재동기화+Zustand `useStream` 스토어)·`lib/order-ticket/`+`OrderTicketModal`(화면공용 quote→commit/정정/취소, FUT paper 실동작·KR/OVS 403→'M4' 안내)·`lib/format.ts`+ui 보강(Loading/Error/Empty/**DeferredBadge**). **화면 6개 실연결**: Overview(`/portfolio`·`/accounts`·`/halt_state`·`/orders/open`, 통화격리 카드·live/paper 분리손익·₩환산 ★별도★)·Positions(`/portfolio/positions?bucket=`, 탭=버킷, 청산→모달, 행클릭→Charts)·Orders(`/orders/open|history`+WS orders/fill, 필터·신/정/취·reconcile, 심볼은 positions로 해석[orders 테이블 symbol 컬럼 없음])·Charts(`/market/ohlcv`+`/market/quote`, **lightweight-charts v5 실캔들**)·Risk(`/risk/limits|events|halt_state`+WS, 킬스위치 L1/L2 2단계 실동작; 한도저장=쓰기 endpoint 없음→읽기전용+M4)·Strategies(백엔드 스텁→정직한 M4 프리뷰). TopBar WS상태/엔진상태 실연결·KILL 버튼→/risk. **시드**: `server/scripts/seed_demo.py`(멱등, positions5/orders6/fills3/risk_events6/bucket_kpi/balances). **검증**: `next build`·`tsc`·`eslint --max-warnings 0` clean / M3 API 모든 UI 엔드포인트 시드데이터 반환 / 시세 라이브(주말 일봉 OHLCV 정상) / **WS OK**(info milestone M3·5토픽·ping→pong) / 6라우트 HTTP200+현재코드+런타임에러0 / CORS(:3000,:13000) 허용. **스크린샷 미수행**(컨테이너에 헤드리스 브라우저 없음 — Playwright chromium 시스템 라이브러리 부족). **알려진 정직한 동작**: 열린주문 빈 것=부팅 reconcile 이 브로커에 없는 시드주문 정리(reconcile 권위)·FUT 새주문은 게이트 REJECT(`per_order_cap_krw=3M`<1계약~1억 명목, e2e 막은 그 캡).
- **M4(LIVE 주문경로) 설계 + 적대검증 2라운드 완료(2026-06-20, 커밋 `4fda9bf`) — 코드 미구현(설계만)**: 신규 `.claude/plans/2026-06-20-M4-계획서.md`(§0~§17). design-first ①상위설계 재독 ②라이브러리 소스 직접검증(4 Explore 에이전트) ③적대 4-렌즈 검증(워크플로우 36에이전트 → 32발견→확정20) → 본문 전부 반영 → ④2차 반영감사(5에이전트, 26항목→OK21/수정5, **미반영 0**). **플랜 TR 매핑 정정 7건 발견(§16)**: OVS 신규=COSAT00301·정정=COSAT00311(통합계획서가 스왑 기재)·COSMT00300=매도상환(취소 아님; 현금취소=COSAT00301 OrdPtnCode'08')·미체결 reconcile=COSAQ00102(COSOQ02701 아님, 그건 예수금)·계좌체결채널=AS1/SC1(GSC 는 시세틱)·KR 종목별잔고=CSPAQ12300·KR 성공코드 side별('00040'매수/'00039'매도, '00000' 아님)·KR/OVS 미체결=list-all. **핵심 설계**: per-bucket EngineState(단일 글로벌 폐기 — live/paper 독립 부팅·격리)·KR/OVS 주문 어댑터 2신규파일·reconcile list-all+tr_cont/cursor 2층 페이지네이션+소멸판정 보류 fail-safe·killswitch LIVE 실동작·2단계 confirm_token(서버측 저장)+첫주문 완충가드·다층 안전캡(HANBIT_ALLOW_LIVE 마스터 + per-order cap + 누적명목 `daily_notional_used_krw` + LIVE orderable 헤드룸). **HIGH 6 결함 설계반영**: 시장가/price=None 캡우회→`NO_NOTIONAL_FOR_LIVE`·누적명목 부재→트랜잭션 캡·OrdNo 일자리셋→date-scoped 키·정정 새OrdNo orphan 오등록→OrgOrdNo 체인·tr_cont 페이지네이션·patch_adapter 갱신(회귀기준 **191** — 1차의 184=grep 과소계수 오류를 2차 감사로 정정). 통합계획서 로드맵에 M4 포인터. **승인+§11[R](캡/집중도/청산정책) 후 M4a 구현 착수**(슬라이스 M4a per-bucket 리팩터[paper무변]~M4f Gate B+소액 e2e).
- **계획 문서 이전·병합 완료(2026-06-20, 사용자 지시)**: `docs/` → **`.claude/plans/`** 로 전체 이전 + **3개 계획서(PLAN/M2_PLAN/M3_PLAN)를 단일 `2026-06-20-통합계획서.md` 로 병합**(내용 verbatim, Part I=전체설계·로드맵 / Part II=M2 / Part III=M3, 상단에 통합 헤더+TOC+참조규약). `STATUS.md`·`UI_WIREFRAMES.md` 는 `git mv` 로 같은 디렉터리 이동. **모든 참조 경로 갱신**: server/app 코드 주석 16파일(`.claude/plans/2026-06-20-통합계획서.md M2/M3 §N`)·`server/README.md`·`web/README.md` 링크·`/catch`·`/recycle`(commit `git add .claude/plans/*.md` 포함)·`.claude/relay/SETUP.md`. 참조 규약: 외부는 `.claude/plans/2026-06-20-통합계획서.md M3 §N`, 문서 내 형제는 상대경로. **pytest 191 passed·ruff clean 회귀 없음**(문서/주석 + 긴 파일명 줄바꿈만). docs/ 폴더 제거됨. **커밋 `b9b9a89`**(git rename 추적: PLAN→통합계획서 76%·STATUS·UI; M2/M3_PLAN 삭제). 적대적 검증 3에이전트(병합 무손실·자동화 정합·참조 무결성) 통과.
- **M0 스캐폴딩 + M1 인증·시세(read-only)**. 커밋 `df54f76`, 브랜치 `feat/m0-m1-scaffold`.
- `main` 은 아직 비어있음(unborn), git **remote 미설정**.
- 3시장(국내·해외주식 실거래 / 해외선물 HKEX 모의) **인증·시세 라이브 검증 PASS**.
- **M2 구현 완료 — 해외선물 HKEX 모의 주문 파이프라인 (paper-only)**. 설계 `2026-06-20-통합계획서.md Part II`(소스 직접 검증 §1 + 4-렌즈 적대 검증 반영). **fake-LS 테스트 green(72 passed) · `ruff check .` clean · 부팅 경로 migration 0002 검증**. 신규/정정/취소/reconcile/킬스위치 + 리스크 게이트 + 상태머신 + 화이트리스트 + API/WS/metrics. **라이브 e2e 1회만 남음**(아래).
- **GitHub 예제 대조 검증 완료(2026-06-18)**: programgarden 공식 예제(`run_CIDBT00100/00900/01000`·`run_o3101`·`run_CIDBQ02400/01500` + **HKEX 주문 `test_order.py`**)와 1:1 대조 — 사용 패턴 일치 확인. **보정 1건**: 어댑터 `ExchCode="HKEX"` 강제 → **미설정**(예제는 HKEX 주문도 ExchCode 비움). 상세 `2026-06-20-통합계획서.md Part II` 상단 배너.
- **M3 설계 완료(2026-06-19) — `2026-06-20-통합계획서.md M3`(v2, design-first, 미구현)**: 포트폴리오 집계 + 위험엔진 확장. **라이브러리 소스 직접 재확인**(account_tracker 시장별 시그니처·정규화 dataclass·금액 `Decimal`·FX 내장(overseas)/미제공(futures)·TC1/2/3 존재) + **4-렌즈 적대적 검증** 반영. 핵심 결정: **범위 M3a(집계·위험코어)/M3b(복구·안전·API) 분할**, EXIT 시장가드 비우회·reduce-only, 버킷격리 구조화(버킷-스코프 repo), 런타임 엔진상태 단일권위(gate+amend+cancel), positions 이중writer 필드분할, quarantine 상태化. PLAN 보정: 금액 minor-unit아님→Decimal/REAL, FX 라이브러리 내장. **구현은 사용자 승인 대기** — 주문 경로 무변(KR/OVS M4까지 부재).
- **M3a 구현 완료 + 적대적 리뷰 반영(2026-06-19, 커밋 `7acc8ad`)**: §14 단계 1~5(집계·위험 코어). **fake-LS 테스트 green(116 passed) · `ruff check .` clean · 앱 import + fresh DB migration 0003 적용 검증**. 신규: `portfolio/{aggregator,fx,metrics,sources/}` · `risk/{caps,daily_loss}` · `models/portfolio_dto` · migration 0003. 확장: `risk/{gate,limits}` · `orders_repo`(필드분할 2-writer·버킷스코프 `positions_for`/`open_orders_for`·balance/kpi/fx/risk_state·`ensure_account`) · `order_service`(fx 주입·버킷스코프 ctx·reconcile 권위 writer) · `mode_matrix`(버킷 헬퍼). **사용자 결정(2026-06-19)**: paper market/currency 집중도 캡 = **1.0(미적용)** — 단일시장·단일통화라 degenerate, LIVE(M4)에서만 의미. **다중렌즈 적대적 리뷰(워크플로우 26 에이전트)**: 22 발견→확정 19→ M3a 범위 실결함 전부 수정(amend 일일halt 우회·EXIT 재분류 position_effect·FX 우선순위① observe·숏 pnl 부호·fx_at_buy·미지원통화 거부·버킷스코프 조회·게이트 노출/max_positions/캡 e2e 테스트). 연기(M3b, 코드 주석): fills 기반 일중 realized(§5.3, 현재 balance 잠정)·amend per_order_cap/INV-7 재검증(§7.1)·to_krw_floor 호출부. **주문 경로 무변**(KR/OVS M4까지 부재).
- **M3b 단계 6 완료(2026-06-19, 커밋 `fe1e325`) — 런타임 EngineState + 부트 스테이트머신 + M3a 연기분 흡수**: **135 passed(116→+19) · `ruff check .` clean · fresh-DB 부트 ACTIVE 검증 · M2 4분기 회귀 green**. 신규: `app/core/engine_state.py`(READ_ONLY|RECONCILING|ACTIVE 런타임 단일권위) · `app/orders/boot.py`(비터미널 4상태 전수분류→boot reconcile→미해소 `quarantined` 격리+critical risk_event→ACTIVE 조건[config PAPER_TRADING ∧ 포지션동기화 성공]) · `app/portfolio/realized.py`(평균원가 실현) · `test_reconcile_boot.py`·`test_realized.py`. 확장: **게이트 step0 + `_get_mutable`(amend/cancel) 이 런타임 EngineState 단일권위 판독**(config `hanbit_engine_state`/`engine_trading_enabled` 게이트 판독 폐기; 거부코드 `ENGINE_NOT_TRADING`→**`ENGINE_NOT_ACTIVE`**) · `quarantined` OrderState(터미널-sink, 운영 수동 resolve)+상태머신 전이 · 게이트 quarantine 시 **ENTRY 차단·감축 EXIT 허용**(`repo.has_quarantined`) · `main.py` lifespan **boot_engine 방어 배선**. **흡수 ①** aggregator `daily_realized_krw` 를 **fills 평균원가 실현**(`repo.realized_pnl_krw`)으로 교체(persist 권위; tick.realized_krw 는 balances 표시값으로 강등) **②** amend `per_order_cap_krw`/INV-7 **재검증**(`RiskGate.check_amend`→`AMEND_REJECTED`, committed 에서 자기주문 제외) **③** `to_krw_floor` 헤드룸 배선(게이트 orderable KRW floor 비교 + `_build_ctx` 잔고 orderable 로드). **주문 경로 무변**(KR/OVS M4까지 부재). **⚠ 알려진 후속 상호작용(단계 8)**: 킬스위치 `cancel_all_open`도 `_get_mutable`(ACTIVE 필요)를 거침 → §8 "우선 레인 예외"(리스크감축 취소 engine-state 우회)는 단계 8에서. 현재 테스트 영향 없음.
- **M3b 단계 7 완료(2026-06-19, 커밋 `9fc5c4d`) — 계좌-TR 직렬 큐(§8)**: **148 passed(135→+13) · `ruff check .` clean · fresh-DB lifespan 부트에서 boot reconcile 의 get_positions 가 실제 큐 경유 확인**. 라이브 실측(M2) 계좌 TR 호출건수 제한(HTTP500 "호출 거래건수를 초과") 대응. 신규 `app/core/tr_queue.py`(`AccountTrQueue`): **baton 패턴**(영구 백그라운드 태스크 없이 대기열 비우는 `submit()` 호출이 leader — lifecycle/close 불필요) · **버킷별 직렬**(동시 in-flight=1) + 최소간격 `HANBIT_TR_MIN_INTERVAL_MS`(신규 설정, 기본 250ms·잠정) · **우선순위** `TrPriority` KILL>BOOT>ROUTINE + **aging**(routine 기아 방지) · **호출건수거부 vs 레이트 구분**(`is_call_count_exceeded`=status500/"거래건수"·"호출건수"만 backoff+재큐, 그 외 전파) · **락순서 불변식**(submit 은 OrderLocks 밖에서만 await) docstring+호출부 주석 박제. 확장: `order_service.reconcile` 의 `get_open_orders`/`get_positions` 를 **큐 경유**(`scope`→priority: boot=BOOT·kill\\*=KILL·그외 ROUTINE) · `OrderService` 에 `tr_queue` 주입 + `_tr_priority` · `config` 에 `HANBIT_TR_MIN_INTERVAL_MS`. aggregator 는 현재 tracker 콜백 **push** 경로라 직접 TR 없음(호출건수 무관) — 향후 폴링 TR 도 큐 경유하도록 주석 명시. **주문 경로 무변.**
- **M3b 단계 8 완료(2026-06-19, 커밋 `ccaf0ca`) — 킬스위치 고도화(§9 + §7.2)**: **159 passed(148→+11) · `ruff check .` clean · app import + fresh-DB(0001~0003) + 빈책 engage/release 스모크 PASS**. 신규 `app/risk/killswitch.py`(버킷-인지 오케스트레이션). **L1 `level1(bucket)`**: **버킷 분기 선행** — LIVE 버킷 = **명시적 no-op-with-warning**(cancel 루프 LIVE open order 진입 0, `kill_switch_live_noop` warn risk_event) · paper 만 실제 취소(M2 `cancel_all_open` 재사용) · **quarantine 노출 분리(§7.2)** OrdNo 보유→raw cancel-by-OrdNo(전송 후에도 상태 `quarantined` 유지=운영 수동 resolve)/OrdNo 없음→`quarantine_excluded` 명시 보고 · **`LIVE_DISABLED` 미삼킴**(paper 취소 루프의 LIVE_DISABLED=라우팅 버그 critical→전파). **L2 `flatten_all_positions`(paper-only fake, §0.3/[검증3])**: `assert bucket=='paper'`+`positions_for('paper')` 만 순회(전수 순회 금지) · reduce-only EXIT(§5.5)→**guarded** `order_service.place` · 멱등키 `flat:{bucket}:{symbol}:{seq}` · 장마감 `pending_flatten`(큐+critical)→`resume_pending_flatten` 가 **현재 포지션 스냅에서 재계산**(verbatim 재발사 금지·이미 flat 드롭·reduce-only 클램프). **단계 6 연기분 흡수(§8 우선 레인)**: `order_service.cancel(risk_reduction=True)`/`_get_mutable(risk_reduction=)` 가 **위험감축 취소에 한해 런타임 ACTIVE 우회**(boot 실패/RECONCILING 에도 취소 성사 — 취소는 멱등·노출감소) · `cancel_all_open` 이 lane 사용 + LIVE_DISABLED 미삼김. `api/risk` 킬스위치 라우트를 `killswitch.engage/release` 로 위임(L1 일원화, **API 계약(scope/action) 무변** — L2/level/confirm_token 노출은 단계 10). **주문 경로 무변.**
- **M3b 단계 10 완료(2026-06-19, 커밋 `df09256`) — API/WS 실데이터화(§11)**: **181 passed(172→+9) · `ruff check .` clean · 7개 라우트 OpenAPI 등록 검증 · KR/OVS 403 유지(orders.py 무변)**. M2 스텁(`portfolio.py`/`accounts.py`) 실데이터화 + 신규 엔드포인트. **모든 엔드포인트는 집계기/reconcile 이 채운 DB 행을 읽기만**(직접 계좌 TR 0 — 호출건수 보호 §11). 구현: ① `GET /portfolio` 버킷 KPI(`bucket_kpi`)+`currency_hhi`+참고 합산(표시 전용 §3·KRW 환산값만) ② `GET /portfolio/positions?bucket=` 버킷-스코프 태깅(eval_krw/fx_now/fx_at_buy), bad bucket→422 ③ `GET /accounts` 시장별 통화별 `balances_snapshot`(+신규 `repo.list_accounts()`) ④ **신규** `GET /risk/halt_state` 버킷별 유효상태(active|halted_daily|killed)+일일손실 진행(realized/eval_loss·baseline·한도) ⑤ **신규** `GET /system/quarantine` quarantined 주문 목록(§7 노출 약속) ⑥ `POST /risk/killswitch` 에 `level:1|2`+`confirm_token` 추가 — **L2 2단계 확인**(토큰 없으면 미실행+발급, 재요청 시 `killswitch.engage_level2`→L1+paper flatten). **L1 하위호환**(level 기본 1) ⑦ `GET /system/health` `engine_state`=**런타임 `order_service.engine.state`**(config 판독 폐기)·`realtime_fills` 노출·**`milestone="M3"`**. 신규 `app/risk/halt.py` `bucket_state`/`states_snapshot` — **킬스위치(trading_halt)가 일일손실(risk_state) 위로 `killed` 강제하는 단일 판정**을 API·WS 가 공유. WS `/stream`: `risk.halt_state` 신규 publish(killswitch engage/release→`_publish_halt`→`states_snapshot`)·`portfolio_snapshot`은 집계기(M3a)가 이미 publish·info 토픽 광고·milestone M3. 신규 `test_portfolio_api.py`(9): /portfolio·/positions(격리·422)·/accounts·/halt_state(일일손실 88/238)·/quarantine·killswitch L2 2단계·health 런타임 engine_state·WS halt_state 생산자 계약. `test_orders_api.py` health 단언 M2→M3 갱신. **주문 경로 무변.** (선택 `risk.feed_health` WS 토픽은 미구현 — §11 optional.)
- **M3b 단계 11 완료(2026-06-20, 커밋 `55ffc34`) — read-only 불변식 진화+메트릭+health/modes+green(§12)**: **191 passed(181→+10) · `ruff check .` clean · app import + fresh-DB(0001~0003) + DoD 테이블 스키마 smoke OK**. §12 마무리 슬라이스 — INV-1 누수를 정적·행동 양쪽에서 차단. ① **`test_readonly_invariant.py` 진화**: 기존 ALWAYS_FORBIDDEN(CSPAT/COSAT/COSMT)·CIDBT* FUT-어댑터-스코프 유지 + 신규 정적 가드 2개 — (a) `test_account_tracker_tr_confined_to_portfolio`: account_tracker 계좌-조회 TR(`CIDBQ03000/01800/o3121`)이 `app/portfolio/` 밖에 등장하면 실패(**forward guard**, 현재 0건; 트레일링 `\b` 제거로 `…InBlock1` 형태까지 잡고 기존 read-path `CIDBQ01500` 오탐 없음 검증) (b) `test_live_stock_adapters_have_no_order_path`: `korea_stock.py`/`overseas_stock.py` 에 `.order()` facade·`place/amend/cancel_order` 메서드 부재(INV-1 구조 증명). ② **`test_no_live_facade.py` 신규(6 tests)**: 행동 증명 — killswitch L1(LIVE)→어댑터 미진입·flatten(LIVE)→assert paper-only·engage(global)→LIVE place/amend 0(위험감축 cancel만)·LIVE tracker 콜백→집계 상태만 갱신(spy)·aggregator tick→주문 0(OrderService 빌드 시 즉시 실패 가드)·paper 대조군. ③ **메트릭/감사로그 보강(실누락 발견)**: `killswitch.engage` 가 `kill_switch` risk_event 외에 **audit_log(`kill_switch.engage`)+`kill_switch_engaged` 카운터** 추가(게이트는 주문 결정만 audit) · `killswitch.release` 는 이전 **무흔적** → `kill_switch_release` risk_event+audit 대칭 트레일 · `boot.py` quarantine → `quarantined` 메트릭 카운터(`/system/metrics` 노출) + 검증 테스트(killswitch 2·boot 1 assertion). ④ **health/modes 정합**: `/system/health`(milestone M3·런타임 engine_state, 단계 10) + `/system/modes`(INV-1 매트릭스 live/live/paper) 일관 — 기존 테스트가 고정, 무변 확인. **변경 파일**: `app/risk/killswitch.py`·`app/orders/boot.py`(런타임 보강) + 테스트 4파일. **주문 경로 무변**(KR/OVS M4까지 부재).
- **M3b 단계 12 완료(2026-06-20) — 문서 마무리(§14 단계 12) ⇒ M3b 종료·M3 DoD 충족·M3 완료**: **191 passed(무변, 문서 위주) · `ruff check .` clean.** ① **`2026-06-20-통합계획서.md M3` §13 open_question 동기화**: 9개 항목 각각 "M3b 코드측 처리 / M4 핸드오프(라이브 미검증·배선)" 상태 박제(코드 주석 일치 확인, 드리프트 0). **신규 발견·명문화**: 집계기 `PortfolioAggregator`·`TrackerSource`·일일손실 `DailyLossMonitor` 는 **유닛-green 컴포넌트로 완성됐으나 account_tracker→집계기→모니터 라이브 push 배선은 M4 로 연기**(`event_bus.py`·`api/ws.py` 주석 "풀 push 는 M3/M5" 근거) → **구동 서버에서 일일손실 halt 자동 발동 안 함**(유닛 `test_daily_loss` 로만 발동 검증, §12 DoD 매핑대로). §13 상단 경계 박스로 명문화. ② **`server/README.md` M2판→M2+M3b 전체 재작성**: 버킷 모델(live=KR+OVS read-only/paper=FUT·INV-1)·engine_state(READ_ONLY→RECONCILING→ACTIVE 런타임 권위 + config 의도·예외 레인)·**엔드포인트 24개 전체 표**(system/market/orders/portfolio/accounts/risk/strategy)·**WS `/stream` 5토픽**·FX(고정 1400/180 fallback·ceil/floor 방향)·**킬스위치 운영 노트**(L1 버킷분기+quarantine raw-cancel/L2 paper flatten 2단계 confirm_token/release 대칭 트레일·수동 resolve)·**⚠ 런타임 배선 경계(M4 연기분)** 섹션·개발/마이그레이션/재개·키 주의. ③ **DoD 공통①~⑤ 최종 점검 통과**: INV(1~7) 정적+행동(`test_readonly_invariant`·`test_no_live_facade`)·감사로그/메트릭(killswitch engage/release audit+`kill_switch_engaged`·boot `quarantined` counter, 단계11)·정적점검(단계11)·open_question(§13, 이 단계)·README(이 단계). **`2026-06-20-통합계획서.md M3` 상단에 🟢 구현완료 배너 추가**. **변경 파일**: `2026-06-20-통합계획서.md M3`(§13+배너)·`server/README.md`(재작성)·`STATUS.md`. **코드 무변·주문 경로 무변**. (단계12 변경은 계획문서 이전·병합과 함께 커밋 `b9b9a89` 에 포함.)
- **M3b 단계 9 완료(2026-06-19, 커밋 `33ed1f3`) — 실시간 TC2/TC3 스캐폴드(§10)**: **172 passed(170→+2) · `ruff check .` clean · fresh-DB 부트 스모크 OK(flag off → no-op)**. 신규 `app/adapters/realtime_future.py` — M2 stub `RealtimeFillSource`(Protocol, `fill_tracker.py`) 구체화: ① 순수 정규화 `tc3_to_fill`(체결→`Fill`, **멱등키 `event_seq='tc:'+ccls_no`** fallback ordr_no) / `tc2_to_event`(접수·거부 ack 관측 dict) — §1.5 코드 매핑 박제(`s_b_ccd` 1=매도/2=매수=어댑터 BnsTpCode 동일 반전, `ordr_ccd` 1=신규/2=정정/3=취소; TC1 미사용) ② `start()/stop()` `.real()` TC2/TC3 구독(라이브 배선, **flag off 기본 no-op**·세션미인증/예외 부팅 비차단) ③ 적재는 **reconcile 과 동일한 단일 writer 경로 `repo.apply_fill(trigger='tc')`** — OrderLocks 단일 writer 락 안(`order_service.locks` property 공유)·미등록 OrdNo 는 생성 안 하고 reconcile orphan 흡수 위임(**reconcile 이 체결 권위**) ④ **`writer_enabled = flag AND 런타임 EngineState.ACTIVE`** — flag 켜도 READ_ONLY/RECONCILING 이면 **writer 강제 off**(검증 Lens2-M3). 신규 config `hanbit_realtime_fills`(기본 off)+`realtime_fills_enabled`. `main.py` lifespan 배선(동일 OrderLocks/EngineState/event_bus 공유·`app.state.realtime_fills`·shutdown stop). **적대적 멀티렌즈 리뷰(워크플로우 17 에이전트, 11발견→확정3 전부 low) 반영**: (a) **TC2 `rsp_cd`/`rsp_msg`/`error_msg` 는 body 가 아니라 `TC2RealResponse` 엔벨로프 필드**(라이브러리 소스 확인) → `_handle_tc2` 가 resp 에서 읽어 주입(어댑터 관례 일치)+테스트 fake 엔벨로프/body 분리로 교정(가짜 green 제거) (b) **TC3 lost-after-terminal 관측**: `apply_fill==False` 두 원인(멱등 중복 vs 동시 reconcile 의 터미널화 후 도착한 진짜 체결 누락)을 **`repo.fill_exists` 로 분기** → `realtime_fills_duplicate` vs `realtime_fills_after_terminal` 전용 메트릭+warn(§13-5 라이브 판단용). 테스트 `test_realtime_future.py`(13). **DoD 아님(스캐폴드, flag off). 주문 경로 무변.**
- **콘텍스트 자동 recycle 이식 완료(2026-06-19, 호스트 핸드오프, 커밋 `a86b979`)**: server 의 메커니즘(=모델 self-clear 아님, watcher 가 `cmux send` 로 `/clear → /catch` 키 주입하는 **외부 오케스트레이션**) 이식 + 독립 세션 정적 검증 통과(`bash -n` OK·`ctl.sh status` req-reset 표시). `ctl.sh recycle[-host]`/`baton-reset` + `watch.sh` `csend()`/`recycle()`/`req-reset` + `config.sh` recycle 변수 + `/recycle`(자동판) + 신규 `/catch`(STATUS.md SSOT 기반 연속, 블로커 우회 금지). **watcher 는 새 코드로 재시작 완료 — 추가 재시작 불필요**(이후 `watch.sh`/`config.sh` 수정 시에만 호스트 `/relay-watcher-restart` 1회). 사용법: 컨텍스트 차거나 작업단위 종료 + 다음작업 있을 때 `/recycle "<다음 작업 한 줄>"`. 상세는 프로젝트 `CLAUDE.md` '팀 협업' 절. **(2026-06-22 org host 전환 — relay 폐기, `/recycle`=`pgtui recycle --self`.)**

## 재개 방법
- **git remote(2026-07-09 실측 정정 — 예전 "remote 없음/main unborn" 기술은 낡음)**: `origin = https://github.com/programgarden/programgarden_hanbit.git` (**공개 repo**). `main` 에 커밋 있음(`f27d712` 등). **커밋 신원은 반드시 `programgarden <programgarden@users.noreply.github.com>`** — 이 repo 의 기존 커밋 전부 이 신원이고, 로컬 `git config user.email` 기본값은 **개인 Gmail 이라 공개 repo 에 새면 안 된다**. repo-local 로 고정해 둠(`git config user.email` 확인). **push 는 사용자가 명시할 때만.**
- 서버 기동: `cd /workspace/server && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`
- 웹 기동:   `cd /workspace/web && pnpm dev -- -H 0.0.0.0 -p 3000`
  - ⚠ 둘 다 **`0.0.0.0` 바인딩 필수**. `127.0.0.1` 이면 도커 포트포워딩이 안 뚫려 호스트에서 연결 거부된다.
- **호스트 접속(2026-07-09 현재 컨테이너 `c7795347f455`)**: web → **http://localhost:13000** · api → http://localhost:18000
  - 정확한 포트는 호스트에서 `docker ps --filter ancestor=hanbit-sandbox:latest` 의 `->3000/tcp` / `->8000/tcp` 로 확인.
- **8000 으로 web 보려면(설정은 이미 커밋됨 `4247ba7`)**: 호스트에서 `docker rm -f <hanbit 컨테이너> && ./sandbox/run-isolated.sh` 로 **재생성**하면 `run-isolated.sh` 가 `-p 8000:3000` publish → http://localhost:8000. **포트 매핑은 컨테이너 생성 시점 고정이라 재생성 외엔 방법 없다.** ⚠ 재생성하면 **컨테이너 안 팀원(e1) claude 세션이 종료**된다(그게 컨테이너 메인 프로세스). `/workspace` 는 바인드마운트라 파일은 보존. ⚠ 임시로 호스트 8000 을 socat 등으로 점유하지 말 것 — 다음 재생성 때 `port_in_use 8000` 폴백이 걸려 8001 로 밀린다.
- **키**: `server/.env`(gitignore, 추적 안 됨)에 LS증권 API 키를 설정한다(`server/.env.example` 참조). `HANBIT_ALLOW_LIVE=false`(실거래 주문 M4까지 차단). 키는 절대 출력/커밋 금지.

## M2 구현 메모 (코드 완료)
- **주문 경로 활성화**: 기본 `HANBIT_ENGINE_STATE=READ_ONLY`(주문 차단). paper FUT 주문 열려면 `.env`/환경변수 `HANBIT_ENGINE_STATE=PAPER_TRADING`. (LIVE 시장 KR/OVS 는 이와 무관하게 M4까지 닫힘.)
- **API**: `POST /api/v1/orders/quote|commit`, `POST /orders/{id}/amend|cancel`, `POST /orders/reconcile`, `GET /orders/open|history`, `GET /risk/limits`, `GET /risk/events`, `POST /risk/killswitch`, `GET /system/metrics`, `GET /system/health`(engine_state). WS `/api/v1/stream` 에 orders/fill/risk_event push.
- **신규 모듈**: `app/adapters/{order_base,order_registry,overseas_future_order}.py`, `app/orders/{state_machine,fill_tracker}.py`, `app/risk/{gate,halt,limits}.py`, `app/services/{order_service,whitelist_service}.py`, `app/core/event_bus.py`, `migrations/0002_m2_paper_orders.sql`.
- **테스트**: `server/tests/test_{order_adapter_future,order_state_machine,risk_gate_paper_fut,order_service,reconcile_future,orders_api,whitelist_o3101}.py` + 진화한 `test_readonly_invariant.py`(KR/OVS 주문 TR 부재 + CIDBT* 어댑터 스코프).
- ⚠ migration 0002 실패 시(중간 ALTER 후 미기록) 복구 = `server/hanbit.db` 삭제 후 재기동(dev, 실데이터 없음).

⚠ **M1 STATUS 정정**: 체결추적 1차는 **`OVC` 가 아니라 reconcile(`CIDBQ02400`)**. `OVC`=시세 틱(market-data), 계좌 주문/체결 채널은 **`TC1`(접수)/`TC2`(응답·체결)/`TC3`(체결)** — 하나 등록 시 전부 자동등록(KR `SC1` 동일). TC2/TC3 값 의미 라이브 미검증 → M2 는 실시간 미배선(reconcile 권위). 주문번호=`block2.OvrsFutsOrdNo`, `BnsTpCode` **1=매도/2=매수**, rsp_cd **`'00000'`=성공**.

## M2 라이브 검증 결과 (2026-06-18)
- **전 파이프라인 라이브 검증 OK**(코드 정상): FUT 모의 세션 인증 → o3101 HKEX 47종목 적재 → engine `PAPER_TRADING` → 리스크 게이트 통과 → 실제 주문 발사(HBIM26 buy limit) → 브로커 응답 → **정확히 rejected 처리**(submitted→rejected). **계좌조회(CIDBQ01500 잔고/CIDBQ02400 미체결)도 라이브 정상**(빈 계좌 0건, 어댑터 `get_positions`/`get_open_orders` 검증). 즉 **주문 수락(계좌 게이트) 한 단계만 빼고 전부 라이브 검증됨.**
- ⛔ **블로커(사용자 액션 필요 — 코드/키로 못 풂)**: 주문 발사(`CIDBT00100`)만 `rsp_cd='01491' "모의투자 주문이 불가한 계좌입니다"`. **`server/.env` 의 `APPKEY_FUTURE` 와 루트 `/workspace/.env` 의 `APPKEY_FUTURE_FAKE` 둘 다 동일 에러.** 로그인·시세·계좌조회는 정상이고 **주문권한만** 없음 → **LS 계좌의 해외선물 "모의투자 주문권한" 신청/활성화 필요**(또는 주문 가능한 모의 키 제공). 활성화 후 `server/tests/`(또는 보관된 e2e 스크립트)로 신/정/취 1회 즉시 통과(코드 변경 0).
- ✅ **확정된 사실(라이브 실측)**: o3101 `gubun="1"`·`ExchCd=="HKEX"`(47종목)·`CtrtPrAmt`=승수 / 계좌번호 **자동주입 동작**(조회·주문 모두 계좌 식별됨) / 성공 `rsp_cd='00000'`·계좌불가 `'01491'` / 계좌 TR **호출건수 제한**(HTTP500 "호출 거래건수를 초과") 있어 연속호출 간 간격 필요(공유 rate_limit_key 미사용 → M3 에서 버킷 직렬 큐).

## M4 결정사항 (2026-07-10, 사용자 확정) — M4 작업 전 필독

> **M4 = 국내주식·해외주식 실거래 주문경로**(INV-1 첫 해제). **해외선물은 M4 이후에도 계속 모의(paper)·HKEX 전용이며 실거래 대상이 아니다**(버킷 `overseas_futureoption` = paper 고정, 게이트가 HKEX 화이트리스트 47종목 강제 — 2026-07-10 사용자 확인). 따라서 M4 가 필요로 하는 실계좌 캡은 **국내 KRW · 해외 USD 둘뿐**이다.

**범위 승인**: 사용자가 "M4f 까지" 선택. ⚠ 이는 **범위(scope) 승인이며 실발사 승인이 아니다.** 계획서 §0.4 ④ / §13("M4f 만 운영자 명시 동의 하에 실거래") 대로, **실제 첫 주문 직전에 별도 명시 승인 + 사용자가 직접 `allow_live=true` 설정**. M4a~M4e 는 `allow_live=false` 로 닫힌 채 코드만 적재 → **실주문 0**.

**[R] 확정분**

| 항목 | 확정값 |
|---|---|
| 집중도 캡 (LIVE) | 단일종목 **0.4** / 단일시장 **0.7** |
| 일일 손실 한도 (LIVE) | **per-order 캡 × 3** |
| L2 청산 주문방식 | **시장가 즉시 청산** (위험 고지 2회 후 사용자 재확인) |

**[R] 확정 (2026-07-11, 사용자 승인) — 명목 캡 잠정 확정**
- **실계좌 실잔고(2026-07-11 e1 조회, `CSPAQ22200`/`COSOQ02701` 실값·seed 아님)**: 국내 KRW 주문가능 **22,334** / 해외 USD 주문가능 **11.27**. ⚠ 극소값(국내 주식 1주도 못 사는 수준) → 사용자가 **"명목 캡 잠정 확정"** 선택.
- **per-order 캡(명목·잠정)**: 국내 **KRW 20,000** / 해외 **USD 10** (실잔고 내). **실주문은 M4f 별도 승인까지 계속 차단** — M4a~e 는 `allow_live=false`, 캡은 코드 적재용. **계좌 충전 후 상향 재조정 예정.**
- **일일 손실 한도** = per-order 캡 × 3 → KRW 60,000 / USD 30 ([R]).
- **`daily_notional_cap`**(§6 step4'' 누적 명목 캡) = per-order 캡 × 3 잠정(KRW 60,000 / USD 30) — **M4b 에서 확정**.
- ⚠ **`GET /api/v1/accounts` 값은 여전히 `seed_demo.py` 데모**(KRW 주문가능 21,400,000 등) — 캡 산정에 쓰지 말 것. 실잔고 DB push(`balances_snapshot`)는 M4d 연기분.

**⚠ L2 시장가 청산 — 위험 고지 + 필수 완화(구현 시 반드시 반영)**
사용자에게 두 번 고지했고 그래도 시장가를 택했다. 위험은 두 겹이다.
1. 계획서 §9 는 KR/OVS 시장가(`OrdprcPtnCode="03"`)의 **수용 여부를 `[L]`(라이브에서만 확인 가능)** 로 남겨뒀다 — 즉 **미검증 경로**.
2. §6 step4' 의 `NO_NOTIONAL_FOR_LIVE` 가드는 명목 산정이 안 되는 LIVE 주문을 **하드 REJECT** 한다. 시장가는 정의상 가격이 없다. §9 도 "청산 명목도 step4' 통과(reduce-only 라도 명목 산정·기록)" 라고 못박았다.

→ 그대로 두면 **킬스위치 L2 가 자기 가드에 막혀 포지션을 못 닫는다** — 안전장치가 정작 위기에 inert 되는 최악의 실패 모드다. **따라서 구현 시 반드시:**
- (a) 청산 주문은 **현재가 스냅으로 명목을 강제 추정**해 step4' 를 통과시킨다.
- (b) 브로커가 `03` 을 거부하면 **지정가+슬리피지로 자동 fallback** — 킬스위치가 절대 no-op 이 되지 않게.
- (c) fallback 발동을 `risk_event` + 메트릭으로 남긴다.
- (d) Gate B(§14)에서 `03` 수용 여부를 `[L]` 실측하고 **결과를 이 절에 박제**한다.

**M4f 전 선행조건**: M3 연기분 라이브 push 배선 인수(M4d 소유 — `balances_snapshot`/일일손실, §6 step7 LIVE orderable 헤드룸의 선행조건). 아래 '다음' 7번 항목.

## 다음
> **(2026-07-15 갱신·팀장 직접구현) M4a(e1, 미커밋)+M4b(KR)+M4c(OVS) 완료 = LIVE 주문경로 배선 끝(allow_live=false 닫힘·실주문 0·pytest 230 green). working tree 에 M4a+M4b+M4c 누적 미커밋 → 사용자 커밋 승인 대기. 다음 순서: ① 커밋(사용자 승인 시) ② M4d(killswitch LIVE 실동작·quote→commit 2단계 confirm_token·첫주문 완충가드·balances_snapshot 라이브 push·step4'' 누적 명목캡) ③ M4e(SC1/AS1 실시간 스캐폴드) ④ M4f(평일+계좌충전+명시 발사승인 → 소액 카나리 e2e; allow_live=false 유지 중). ⑤ 책 반영(ch07 §7.2.5·§7.4·ch08, 별도 책 세션).**
> **(2026-07-11 갱신) ① `/charts` 정렬 버그 = e1 수정·커밋됨(`9ec884a`). ② M4 per-order 캡 = 명목 잠정 확정(위 '## M4 결정사항' [R]). ③ KR 대체거래소 NXT = programgarden_finance `MbrNo`('KRX'/'NXT') 지원 확인 → KR 어댑터가 `intent.exchange="NXT"` 로 라우팅(정규장 15:30 종료 후). OVS 국내주간거래 개장시간 주문수용은 [L](라이브 관측).**
> **(2026-07-11 저녁 — 사용자 지시·방향전환) UI 의 "M4 예정" 라벨을 문구만 바꾸지 말고 *실제로 구현*하라. 시장 마감(토요일)은 구현을 막지 않음** — M4a~e 는 `allow_live=false` 로 지금 전부 코딩·fake-LS 테스트 가능(실주문 0). **시장 개장+계좌충전(실잔고 KRW 22,334/USD 11.27 극소)+명시 발사승인은 M4f(실발사)만의 게이트다.** "M4 예정" 이 붙어 있던 건 게으름/시장 때문이 아니라 M4=최초 실거래(INV-1 첫 해제)라 설계+적대검증+안전게이트 뒤로 의도적 단계화했기 때문(그 설계·검증은 끝, 이제 구현). → **드라이브(팀장): e1 이 M4a DoD 후 M4b(KR)→M4c(OVS)→M4d(안전UI+라이브배선)→M4e(실시간스캐폴드) 순차 구현, 각 슬라이스 fake-LS green + 해당 UI 배지 제거(Q2: 코드 주석 M4 도 정리). M4f 는 평일·충전·명시승인까지 HOLD(allow_live=false 유지).** ⚠ **전략 엔진(Strategies)·실시간 시세 틱/호가(Charts)는 M4 계획서(=주문경로) 밖 = 별도/후속** — 전략엔진은 설계 미착수(자동발주라 자체 안전설계 필요, 사실상 M5); 잔고/PnL push 는 M4d 포함, 시세 틱 실시간은 별도. 주문경로(M4b~d) 완료 시 주문/포지션/LIVE킬스위치/리스크게이트 배지가 실제 소멸.
> **현재 사용자 우선순위 = UI(사용자 지시 "우선 ui부터 완성하자"). 실거래(M4)는 평일에**(주말이라 보류). **이 프로젝트는 교육용 예제 → 작업 마무리되면 교재 작업 예정**(코드·화면이 읽기 쉽고 교육적이어야 함). 메모리 스토어 깨져 있어 STATUS 가 SSOT.
> ✅ **(완료, 2026-06-20) HTTP 404 호스트 육안확인** + ✅ **(완료, 2026-06-21) UI 마무리 Wave 1**(명명갭 3/4 + 버그, 커밋 `fa06753`, 위 '완료된 것' 1번).
1. **(1순위·사용자 지정, 2026-07-09) `/charts` 런타임 에러 수정 — 캔들이 시간 내림차순이라 lightweight-charts 가 assert**. **증상**: 브라우저 `/charts` 에서 `Assertion failed: data must be asc ordered by time, index=1, time=1783468800, prev time=1783555200` @ `web/src/app/charts/ChartsView.tsx:247` 의 `candleRef.current.setData(...)` (`CandleChart.useEffect`). 두 값 차 = 86400초(일봉 1개)이고 **prev > current** → 데이터가 **최신순(desc)**. (`1783555200`=2026-07-09, `1783468800`=2026-07-08.) SSR 은 200 이고 클라이언트 렌더에서만 터진다.
   - **근본원인(코드 확인 완료)**: **정렬이 어디에도 없다.** `server/app/api/market.py:39 market_ohlcv` → `server/app/services/market_service.py:41 get_ohlcv` 는 **단순 위임**이고, 어댑터 3개(`app/adapters/{korea_stock,overseas_stock,overseas_future}.py` 의 `get_ohlcv`)는 브로커 응답 순서(LS 일봉 = 최신순)를 그대로 반환. lightweight-charts `setData()` 는 **시간 오름차순 필수**.
   - **다음 행동 1가지**: `server/app/services/market_service.py:41 get_ohlcv` 의 **반환 직전에 `date` 오름차순 정렬** — 어댑터 3개가 모두 지나는 **단일 초크포인트**라 한 곳만 고치면 전부 해결된다. 추가로 방어적으로 `web/src/app/charts/ChartsView.tsx:246` useEffect 안에서 `[...candles].sort((a,b)=>a.date.localeCompare(b.date))` 한 뒤 `setData` 에 넘긴다. (`Candle.date` 는 `YYYYMMDD` 고정폭 → 문자열 정렬 = 시간순. `models/dto.py:28` 이 "YYYYMMDD 등" 이라 하니 분봉 도입 시 폭 확인할 것.)
   - **⚠ 함정**: `count` 는 "최신 N개"를 받는 의미다. **자른 뒤에 정렬**해야 최근 N개가 오름차순으로 남는다. 정렬 후 자르면 가장 **과거** N개가 남아 조용히 틀린 차트가 된다.
   - **검증**: ① 컨테이너 안에서 `curl -s 'localhost:3000/api/v1/market/ohlcv?market=<m>&symbol=<s>&period=D&count=100'` → `data.candles` 의 첫 `date` < 끝 `date` ② 호스트 브라우저 `/charts` 런타임 에러 0(하드리프레시) ③ `pytest`(191 기준)·`tsc --noEmit`·`eslint . --max-warnings 0` clean.
2. **(2순위·사용자 승인 2026-07-10) M4 착수 — 먼저 `M4a`**. 계획서 `.claude/plans/2026-06-20-M4-계획서.md` §13 슬라이스. **M4a = per-bucket EngineState 리팩터(§3) + config 토글(§2) + DTO 보강(§4.3) + `patch_adapter` allow_live 수용(§3.4)**. 주문경로 **무변**(paper 동작 보존, 실주문 0). **[R] 값과 무관하므로 지금 바로 착수 가능.** DoD = **191 회귀 green** + per-bucket 전이 테스트(live 부트실패 → live `READ_ONLY` ∧ paper `ACTIVE` 격리, §17 L3-11). **아래 '## M4 결정사항' 절을 반드시 먼저 읽을 것** — [R] 확정분·미확정분·안전 게이트가 거기 있다.
   - ⚠ **M4b 부터는 per-order 캡([R]) 없이 못 쓴다.** 캡 확정 전까지는 M4a 만.
   - ⚠ **M4f(실발사)는 별도 명시 승인 필요.** 사용자의 "M4f까지" 는 **범위 승인**이지 발사 승인이 아니다(계획서 §0.4 ④·§13: "M4f 만 운영자 명시 동의 하에 실거래").
3. **(3순위·사용자 지정) 교재 작성 — ⚠ 별도 비공개 repo 에서 진행**: 책은 이 예제 repo 와 분리된 별도 비공개 저장소에서 작성한다(예제 repo = 읽기전용 참조·시크릿 인용 금지). 구조=챕터1개=파일1개 + manifest SSOT 인덱스 + outline 골격 초안(검토 대기). **이 hanbit 세션에서는 책을 쓰지 않는다**(별도 세션). 교재가 인용할 UI 현재상태 = `2026-06-21-UI-마무리-작업목록.md` '화면별 현재상태'.
4. **(4순위·사용자 지시 시) UI Wave 2~4** — `2026-06-21-UI-마무리-작업목록.md` 참조. Wave 2=교재화(화면 내 교육 설명+코드 주석, educational '고' 다수)·Wave 3=와이어프레임 복원(Overview 분배도넛·위험KPI, Strategies 3패널, Risk scope 그룹핑)·Wave 4=폴리시·버그. ④ Charts SL/TP 오버레이 등 `[M4]` 항목은 보류 유지.
   - **✅ Wave 1 코드 커밋됨(`fa06753`, 9파일)**. push 는 remote 미설정으로 보류(STATUS '재개 방법' 의 GitHub push 보류 항목 참조).
   - **참고(정직한 동작, 버그 아님)**: 열린주문 빈 것=부팅 reconcile 정리 / FUT 새주문 게이트 REJECT=`per_order_cap_krw=3M`<1계약~1억(이건 §11[R]/M4 전 캡 재검토 대상, e2e 도 막은 캡).
5. **(5순위·평일) 실전 라이브 테스트 — M2 해외선물 paper e2e**: `cd /workspace/server && PYTHONPATH=/workspace/server uv run python scripts/live_e2e_paper_fut.py` (신/정/취+reconcile). ⚠ 그냥 실행 시 `ModuleNotFoundError: app`(scripts 가 sys.path) → `PYTHONPATH=/workspace/server` 필요. **블로커 2개**: ① 주문권한 `01491`(LS 모의 주문권한 미활성, 사용자 액션) ② **게이트 `per_order_cap_krw=3M` 이 HBIM26 1계약(~1억 명목)을 먼저 REJECT** → 브로커 도달 못 함(가격 11000×50×180≈1.01억≫3M). 평일+권한 활성+캡 상향(또는 소액명목) 필요. 우회 금지.
   - **⚠ 블로커(사용자 액션 — 코드/키로 못 풂)**: 직전 라이브 실측에서 **주문 발사만** `rsp_cd='01491' "모의투자 주문이 불가한 계좌입니다"` — LS 해외선물 **모의투자 주문권한** 미활성. 로그인·시세·계좌조회(CIDBQ01500/02400)는 라이브 정상. **사용자가 주문권한을 활성화했으면 스크립트가 즉시 신/정/취 통과**(코드 변경 0). 아직이면 `01491` 재현 여부만 확인해 **"주문권한 여전히 미활성"** 한 줄 보고하고 멈춘다(블로커 우회 금지).
   - 사전 점검: `server/.env` 에 키 채워져 있음(추적 안 됨, 출력 금지). engine `PAPER_TRADING` 필요(스크립트가 설정하거나 `HANBIT_ENGINE_STATE=PAPER_TRADING`). HKEX o3101 마스터 적재 경로 포함. 계좌 TR 호출건수 제한 있어 연속호출 간격 주의(§8 tr_queue).
   - **M4 는 live 테스트 불가**: 설계+적대검증만 완료, 코드 미구현 → 실행 가능한 실전 테스트는 이 M2 paper FUT e2e 가 **유일**.
6. **(슬라이스 상세 — 2026-07-10 승인 완료, 착수는 위 2순위)** M4a 구현: `.claude/plans/2026-06-20-M4-계획서.md` 의 ④ 사용자 승인 + §11 [R] 값 확정(국내/해외 캡·집중도·청산 시장가) 후 §13 슬라이스 **M4a**(per-bucket EngineState 리팩터 + config 토글 + DTO + `patch_adapter` 갱신; **paper 동작 무변·[R] 무관 → 값 없이도 착수 가능**, **191 회귀 green** 목표). M4b~f(KR/OVS 어댑터·안전 UI·실시간·Gate B)는 [R] 필요. **⚠ 최고위험: M4 가 INV-1 첫 해제(실제 돈)** — HIGH 6 결함은 M4계획서 §0.2/§6/§7/§1.5 에 설계반영됨.
7. **⚠ M4 진입 전 인수할 M3 연기분**(통합계획서 M3 §13 + M4계획서 §15): 라이브 push 배선(`main.py` lifespan account_tracker→aggregator→`DailyLossMonitor`→persist; **balances_snapshot/orderable 헤드룸 포함 — M4d 소유, LIVE 활성 선행조건**)·LIVE 2-tracker 머지·라이브 값 검증(FX·TC·호출건수·거래일경계·청산 시장가). 핵심 보정: 금액 `Decimal`/`REAL`·고정환율 1400/180 fallback·positions 권위=reconcile·API 는 DB 행 read-only(직접 계좌 TR 금지)·판정 단일권위 `app/risk/halt.py` `bucket_state`(킬스위치 trading_halt > 일일손실 risk_state).

> 세션 리사이클: `/recycle` 명령 추가됨(`.claude/commands/recycle.md`) — 컨텍스트 30~50%/세션 종료 시 commit+STATUS 핸드오프 후 `/clear`. 라이브 e2e 재현 스크립트: `server/scripts/live_e2e_paper_fut.py`.

## 라이브러리 핵심 사실 (검증 완료)
`2026-06-20-통합계획서.md` 부록 A + `server/app/core/sessions.py` 참조:
- 시장별 **별도 `LS()` 인스턴스**(`LS.get_instance()` 금지) · `SetupOptions(on_rate_limit="wait")` 를 **요청마다** 전달.
- 해외선물 현재가=`o3105`(o3101=마스터), 일/주/월봉=`o3108`. 국내 t1102/t8451, 해외주식 g3101/g3103(`sign`+절대값 `diff`).
- programgarden 이 `websockets<16` 요구 → 15.0.1(uvicorn 회귀 무해 확인).

설계/도면: `2026-06-20-통합계획서.md`, `UI_WIREFRAMES.md`. 재사용 워크플로우: `.claude/workflows/_plan-doc.js`, `_ui-wireframes.js`.
