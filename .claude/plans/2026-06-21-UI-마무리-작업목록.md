# UI 마무리·교재화 작업목록 (2026-06-21)

> **목적**: 교육용 대시보드(web/)를 와이어프레임(`UI_WIREFRAMES.md`)에 맞춰 마무리하고 **교재화**(코드·화면이
> 읽기 쉽고 교육적)하기 위한 단일 작업목록. 화면별 적대적 분석(워크플로우 8에이전트: 6화면 + 공통셸/모달 +
> 명명갭/백엔드 정합)에서 도출. **교재는 이 문서의 '화면별 현재상태'를 챕터 기준으로 삼는다.**
> 태그: `[교재화]` 교육 설명/주석 · `[와이어]` 와이어프레임 갭 · `[폴리시]` UX·접근성 · `[버그]` · `[M4]` 주문경로/실데이터 의존(보류).
> effort=S/M/L · value=고/중/저.

---

## Wave 1 — 완료 (2026-06-21, 커밋 `fa06753`)

명명 미구현 잔여 4개 중 **3개 마감 + 고가치 버그 1개**. 검증: `ruff` clean · `tsc` clean ·
`eslint --max-warnings 0` clean · 서버 **191 passed** · 새 엔드포인트 200(프록시) · 6라우트 200.

- **③ 서버시계 배선** — `ServerClock`이 `new Date()` 로컬시각 → `/system/clock` **서버 권위시각 + skew 보정**
  (30s 폴링으로 드리프트 재보정). 시장세션은 백엔드가 M0 스텁이라 정직하게 `세션 미상 (M0)` 표기.
  파일: `web/src/components/ServerClock.tsx`.
- **① HKEX 화이트리스트 검증** — 신규 `GET /orders/whitelist?market=`(게이트가 강제하던 화이트리스트 노출,
  multiplier 는 meta_json 파싱; 현재 시드엔 meta 없어 null). 모달 심볼이 **자유텍스트 → 검증 select**(47종목).
  화이트리스트 외/빈심볼/수량≤0/지정가 가격≤0 → 견적 버튼 차단 + 인라인 사유.
  파일: `server/app/api/orders.py`, `web/src/components/OrderTicketModal.tsx`, `web/src/lib/api/{types,endpoints}.ts`,
  `web/src/lib/query/{keys,hooks}.ts`.
- **② Orders 페이지네이션** — history 80건 고정 → **"더 보기 (+50)"**(서버 limit/offset 활용, limit 을 쿼리키에 포함).
  + 필터 0건 vs 데이터 0건 구분 + **"필터 초기화"** CTA. 파일: `web/src/app/orders/page.tsx`, `web/src/lib/query/hooks.ts`.
- **🐛 Positions 시세없음 버그** — `current_price=null` 행이 합계를 조용히 0으로 왜곡 → **'시세 없음' 명시 +
  평가/손익 합계에서 제외 + ⚠경고**(매입가만 반영). 파일: `web/src/app/positions/PositionsView.tsx`.

**남은 명명갭 = ④ Charts SL/TP 오버레이뿐 → 데이터 경로가 M4라 보류가 맞음**(분석 동일 판정).

---

## 남은 작업 (웨이브)

- **Wave 2 — 교재화**: 화면 내 교육 설명 노출 + 핵심 로직 설명주석. 분석에서 educational 'high' 다수. **교재 작업과 직접 맞물림.**
- **Wave 3 — 와이어프레임 복원**: Overview 분배도넛·위험KPI, Strategies 3패널 복원, Risk scope 그룹핑 등 큰 갭.
- **Wave 4 — 폴리시·버그**: 정렬·접근성·로딩표시·캐시 무효화 버그.
- **M4 보류**: 주문경로/실데이터 의존 — 지금 구현 불가, 화면엔 정직 배지 유지.

---

## 화면별 현재상태 + 남은 작업 (교재 챕터 기준)

### 1. Overview (`web/src/app/page.tsx`)
**현재**: KPI 4칸(활성전략 M4배지/열린주문/포지션 live·paper/위험상태) · 통화카드 3장(KRW/USD/HKD, **절대 비합산**) ·
live/paper 분리손익 · ₩환산 총액 ★별도★ 참고박스 · 하단 WS 수신상태. 섹션별 독립 로딩/에러격하.
- `[와이어]` 시장별 **분배 도넛**(KR/OVS/FUT 비중%) — 데이터 이미 옴(`buckets[*].by_market/by_currency`). SVG/CSS 비율바. **M·고**
- `[와이어]` 위험상태 KPI에 **MDD·한도 예산** 노출 — `drawdown_pct`·`risk_budget_left_krw` 이미 옴. **S·고**
- `[와이어]` KPI 타일 **클릭→화면 이동**(활성전략→/strategies 등). **S·중**
- `[와이어]` 일중 PnL **스파크라인** 자리표시(시계열은 `[M4]`, DeferredBadge로 골격만). **S·저**
- `[교재화]` 통화 **비합산 이유**(환율변동·버킷격리) 화면 1줄 + `totals_note` 한글화(서버가 영문 반환) · riskSummary 상태정의 tooltip · `ccyGroup`/`CCY_CARDS`(통화↔버킷 고정 불변식)/`riskSummary` 주석 · `mult`→`multiplierOf`. **S·중**
- `[폴리시]` 실패 KPI '⚠'를 큰 숫자자리 대신 **경고 칩**으로 · 빈 통화카드 격하 · 로딩 스켈레톤. **S·중**
- `[M4]` ₩박스 FX 환율·환산시각(FX 엔드포인트 부재).

### 2. Positions (`web/src/app/positions/PositionsView.tsx`)
**현재**: 3탭(KR/OS live·FUT paper) 테이블 · 통화별 합계 + ₩환산 참고합계 · 행클릭→Charts · 청산→공유모달(FUT만 실동작, LIVE는 M4). **Wave1: 시세없음 처리 완료.**
- `[교재화]` 화면 내 **버킷격리·다통화 비합산·₩환산=표시전용** 설명 패널 · `multiplier`/`fromRatio`/INV-1 차단사유 주석 · 헤더 한글병기. **S·고**
- `[와이어]` 청산 모달에 **예상 실현손익·예상 회수액** 전달·표시(`pnl_amount`/`pnl_rate`/회수액). **M·고**
- `[와이어]` 카드 헤더 **WS 연결배지**(`useStream.status`) + 새로고침 `isFetching` 표시(폴링 스냅샷 정직성). **S·중**
- `[와이어]` ₩환산 합계에 **환율(`fx_now`)·환산시각·환산 손익합계** 부가표기. **S·중**
- `[폴리시]` 컬럼 헤더 클릭 **정렬** · 행클릭 발견성(hover 단서). **M·중**
- `[M4]` LIVE 청산 2단계 확인(확인문구 타이핑 + 동의) — 실행은 M4까지 차단, 도면 일치용 시각 골격만 선택적.

### 3. Orders (`web/src/app/orders/page.tsx`)
**현재**: open/history 테이블 + WS 타임라인 · 시장/상태/심볼 3필터 · 행별 정정/취소·새주문·reconcile. **Wave1: 페이지네이션·필터CTA 완료.**
- `[교재화]` 화면 내 설명(버킷격리 INV-1·LIVE read-only 이유·symbol 을 positions 에서 조립하는 이유·quote→commit 2단계) · `matches`/`symMap`/`newOrder` 하드코딩 주석. **S·고**
- `[와이어]` **서버측 필터**(market/status/symbol) — 서버 `/orders/history` 가 limit/offset 만 받음. 페이지 경계 후 클라필터 누락 해소하려면 서버 쿼리 확장. **M·중**(서버+웹)
- `[와이어]` 만료/타임아웃 **사유 구분 라벨**(휴장 vs 호가미도달) · 체결금액(qty×price) 표기. **M·중**
- `[폴리시]` 필터 select/검색 **label/aria** + 테이블 헤더 title 툴팁(체결/잔량·OrdNo) · search debounce · WS 리스트 key index→이벤트 id. **S·중**
- `[M4]` amend/cancel LIVE 2단계 확인.

### 4. Charts (`web/src/app/charts/ChartsView.tsx`)
**현재**: 3컬럼(워치리스트 5하드코딩/캔들/우측 현재가) · 일·주·월 캔들(lightweight-charts v5) · quote 등락. 호가/틱/오버레이는 M4 배지.
- `[교재화]` **항상 보이는 '일봉 스냅샷(D/W/M) — 분봉·실시간 추종은 M4' 배너**(현재 에러시에만 노출). **S·고**
- `[교재화]` `CandleChart` **명령형↔선언형 브리지** 주석 확장(createChart 1회/setData 갱신·cleanup·ResizeObserver) · WATCHLIST 하드코딩 이유(`/market/symbols` 미구현). **S·중**
- `[와이어]` 워치리스트 **LIVE/FUT(HKEX) 구분선** · 심볼검색/시장토글(5종목 내 클라필터부터) · 하단 **OHLC 메타** 행. **S~M·중**
- `[폴리시]` quote 로딩/에러 격하(휴장 침묵 방지) · 차트색 16진 하드코딩 → CSS 변수 토큰화 · 기간탭 `isFetching`·`aria-current`. **S~M·중**
- `[M4]` **④ SL/TP·마커·미체결 지정가 라인 오버레이**(토글 ◉마커 ◉SL/TP 포함) — SL/TP=Risk연동·마커=WS fill 둘 다 M4. **단 미체결 지정가 라인은 `useOpenOrders`(이미 존재)로 부분 선구현 가능**(createPriceLine 점선). 호가창·체결틱=WS M4.

### 5. Strategies (`web/src/app/strategies/{page,StrategyCards}.tsx`)
**현재**: 백엔드 스텁(전략엔진=M4)이라 **전체가 정직 비활성 미리보기**. 카드 grid(opacity-60) + "M4" 안내.
- `[와이어]` **할당 합계 배너**(통화별 ₩/$/HK$ vs 가용자본 + OK/⚠ 배지, 비활성). **M·고**
- `[와이어]` **실행 로그 패널**(레벨 탭 signal/order/skip/error/risk·tail-follow·block 강조, 정직 골격 + 'WS 연결 M4'). **M·고**
- `[와이어]` **활성 한도 미니패널 복원**(소액상한/누적명목/일손실/킬스위치 4행, 각 항목 설명). 현재 한 줄로 붕괴됨. **M·고**
- `[교재화]` 정직 미리보기 원칙(스텁→가짜전략 안 띄움) 화면 1줄 + PREVIEW 옆 '와이어 예시·실데이터 아님' 주석 + 연결예정 endpoint 주석. **S·고**
- `[와이어/폴리시]` [+새 전략] 비활성 버튼 · 전략표본을 와이어 정전(momo-kr/pairs-os/fut-hk)으로 교정 · StatusChip 통일 · DeferredBadge 중복 제거 · alloc 을 `format.ts` 경유. **S·중**

### 6. Risk (`web/src/app/risk/{page,KillSwitchPanel}.tsx`)
**현재**: 한도 읽기전용 dl("저장 M4") · 킬스위치 **L1/L2 2단계 실동작**(global, paper만 실청산·LIVE no-op) · 위반로그(폴링 `/risk/events` + WS `risk_event` + severity 필터).
- `[교재화]` 킬스위치 **L1/L2·confirm_token·fail-closed 개념 화면 설명**(접이식 안내/보조텍스트) · 한도별 작동방식 1줄 · halt 상태 범례(halted_daily vs killed). **M·고**
- `[와이어]` 한도 **scope/통화/세션 그룹핑**(평탄 Record → 카테고리 + 통화기호·세션 KST/美ET/HKEX HKT, 소액상한 🔒행). **L·고**
- `[와이어]` 위반로그 **레벨 4종**(info/warn/block/killswitch) + **source 표기**(전략/엔진/사람) · 시장별 동결 토글(KR/OS/FUT) · 한도상태 요약헤더. **M·중**
- `[버그]` 헤더 halt 를 `useHaltState` 로 일원화(`useRiskLimits.data.halt` 최초1회라 stale) · killswitch 성공 시 `portfolio`/`positions` 캐시도 무효화(L2 청산 후 stale). **S·중**
- `[와이어]` 위반로그 **페이지네이션**(`getRiskEvents` offset 이미 지원, Orders 패턴 재사용) · WS 라이브스트립↔폴링목록 중복 정리. **M·중**

### 7. 공통 App Shell + 주문확인 모달 (`web/src/components/*`)
**현재**: TopBar(ModeBadge `/system/modes`·ConnectionStatus 실 WS+엔진상태·ServerClock**[Wave1 배선됨]**·KillButton→/risk) · SideNav 6라우트 · 모달 4흐름(new/liquidate quote→commit·amend·cancel; LIVE는 403 격하). **Wave1: 화이트리스트 검증·입력검증 완료.**
- `[교재화]` 모달 new 흐름에 **'왜 견적(quote)을 먼저 받나 = 서버 리스크 게이트 사전검증'** 1줄 · ServerClock '서버 권위시각' 캡션 · ConnectionStatus 색 범례(open/ACTIVE/READ_ONLY 의미) · `ran.current` 가드 주석. **S·중**
- `[폴리시]` 모달 초기 포커스 + 포커스 트랩 + backdrop 비차단 안내 · ConnectionStatus enum 한글화 · confirm_token 말꼬리 노출 축약. **S~M·중**
- `[M4]` 견적 **est 블록**(명목/수수료/체결후 노출 — 백엔드 `QuoteResponse` 확장 필요) · **expires_in 카운트다운** · 5축 리스크 체크라인 · LIVE 2체크박스.

---

## M4 보류 (지금 구현 불가 — 주문경로/실데이터 의존)
Charts ④ SL/TP·마커·호가·체결틱 · 일중 PnL 스파크라인 · 견적 fee/exposure · expires_in · LIVE 2단계 **실행** ·
FX 엔드포인트 · 전략 실데이터·실행로그 라이브 · 실시간 push 배선(account_tracker→집계기→일일손실, 통합계획서 M3 §13).
**원칙**: 화면엔 `DeferredBadge`/정직 문구로 '왜 지금 이렇게 동작하는지' 노출 유지(교육 정직성).

## 참고
- 와이어프레임 라인참조·도면: `.claude/plans/UI_WIREFRAMES.md`.
- 상위 설계: `.claude/plans/2026-06-20-통합계획서.md`(Part I §6 UI 개요 / Part III M3 API).
- Wave 1 검증 로그·분석 원본은 휘발성(워크플로우 /tmp) — 이 문서가 영속 요약.
